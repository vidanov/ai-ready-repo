description: >
  Add a Money.subtract() method that validates currency matching
  and raises ValueError for negative results.

verification: "python -m pytest tests/unit/test_domain_money.py -q"
expected_exit_code: 0

hints:
  - "The method should follow the same pattern as Money.add()"
  - "A subtraction that produces a negative amount should raise ValueError"
  - "Run: make test-unit to verify"
